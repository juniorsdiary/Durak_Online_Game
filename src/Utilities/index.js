import { getWidth, convertTime } from './functions';
import useForm from './hooks';
const breakPoints = {
  xs: 560,
  sm: 780,
  md: 960,
  lg: 1100,
};
export { getWidth, convertTime, breakPoints, useForm };
